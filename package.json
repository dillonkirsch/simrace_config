{
  "name": "sim-controls-manager",
  "version": "0.0.0",
  "private": true,
  "description": "Safe, preview-first management of racing simulator button bindings.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "catalog:validate": "node src/cli.js catalog validate",
    "test": "node --test"
  }
}
